using System.Collections;
using System.Collections.Generic;
using DIALOGUE;
using UnityEngine;

namespace TESTING
{
    public class Testing_DialogueFile : MonoBehaviour
    {
        // [SerializeField] private TextAsset dialogueFile;
        // private bool checkStart;

        // public void StartConversation()
        // {
        //     checkStart = true;
        //     if (checkStart == true)
        //     {
        //         List<string> lines = FileManager.ReadTextAsset(dialogueFile, false);
        //         DialogueManager.instance.Say(lines);
        //     }
        //     checkStart = false;
        // }

        // private void Update() 
        // {
        //     if(Input.GetKeyDown(KeyCode.Space) && DialogueManager.instance.isRunningConversation) 
        //     {
        //         DialogueManager.instance.OnDialogue_Next();
        //     }
        // }
    }
}